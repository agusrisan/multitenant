/// Modules root
///
/// Contains all feature modules organized following DDD principles.
/// Each module contains its own domain, application, infrastructure,
/// and interface layers (web/api).

pub mod auth;

// Future modules:
// pub mod user;
// pub mod organization;
